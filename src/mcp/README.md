# MCP Server Modules

This directory is for MCP (Model Context Protocol) server implementations.

## Expected Structure

- `src/mcp/<server-name>/` - Individual MCP server implementations

## Purpose

MCP servers provide tools and resources to OpenCode agents.

See `AGENTS.md` for MCP development guidelines.
